services:
  headscale:
    image: headscale/headscale:0.28.0
    restart: unless-stopped
    command: serve
    volumes:
      - ./config.yaml:/etc/headscale/config.yaml:ro
      - ./acl.json:/etc/headscale/acl.json:ro
      - headscale-data:/var/lib/headscale
    ports:
      - "127.0.0.1:8080:8080" # control plane; arahkan Cloudflared ke host lokal port 8080
    environment:
      - TZ=Asia/Makassar

volumes:
  headscale-data:
